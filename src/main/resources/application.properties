#application-details
spring.application.name=masterservice

#server
server.port=8888

#Register with eureka
#eureka.client.register-with-eureka=true
##register it-self with eureka server
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka/

#datasource
spring.datasource.url=jdbc:mysql://localhost:3306/master?createDatabaseIfNotExist=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=sahu


#log JPA queries (should comment at production)
spring.jpa.show-sql=true


# Drop and create new tables (create, create-drop, validate, update)
# Only for testing purposes - comment this in production
spring.jpa.hibernate.ddl-auto=create


#User Authantication
spring.security.user.name=chitra
spring.security.user.password=sahu

#actuator
#http://localhost:8881/actuator
management.endpoints.web.exposure.include=*



